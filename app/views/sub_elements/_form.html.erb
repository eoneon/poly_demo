<%= form_for parent do |f| %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="row">
    <div class="col-md-2">
      <%= f.fields_for :field_groups do |builder| %>
        <%= render 'sub_elements/field_group_fields', f: builder %>
      <% end %>
      <%= link_to_add_fields "Add Field", f, :field_groups, "sub_elements" %>
    </div>
  </div>

  <!-- <div class="row">
    <div class="col-md-2">
      <%#= f.fields_for :item_fields, @medium.item_fields.build do |builder| %>
        <%#= render 'media/item_field_fields', f: builder %>
      <%# end %>
      <%#= link_to_add_fields "Add Field", f, :item_fields, "media" %>
    </div>
  </div> -->
  <%= f.submit "Save" %>
<% end %>
